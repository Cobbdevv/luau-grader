name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build-cli:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo build --release -p luau-grader-cli
      - uses: actions/upload-artifact@v4
        with:
          name: luau-grader-cli-windows
          path: target/release/luau-grader.exe

  build-tauri:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cargo install tauri-cli --version "^2"
      - run: cargo tauri build
      - uses: actions/upload-artifact@v4
        with:
          name: luau-grader-installer-windows
          path: |
            target/release/bundle/msi/*.msi
            target/release/bundle/nsis/*.exe

  release:
    needs: [build-cli, build-tauri]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/luau-grader-cli-windows/luau-grader.exe
            artifacts/luau-grader-installer-windows/**/*
